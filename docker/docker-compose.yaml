services:
  backend:
    build:
      context: .
    volumes:
      - ..:/app
    working_dir: /app
    command: sleep infinity

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: app
    ports:
      - "5432:5432"
